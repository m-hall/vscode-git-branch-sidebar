export enum StashCommands {
    show = 'scm-local-stashes.stash.show',
    apply = 'scm-local-stashes.stash.apply',
    pop = 'scm-local-stashes.stash.pop',
    drop = 'scm-local-stashes.stash.drop'
}
